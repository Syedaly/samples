s3://<bucket-name>/iceberg-tables/<table-name>/
    ├── metadata/
    │   ├── version-<timestamp>.metadata.json  (metadata snapshots)
    │   ├── version-<timestamp>.metadata.json  (previous versions)
    ├── data/
    │   ├── <partition-key>=<value>/
    │   │   ├── <filename-1>.parquet
    │   │   ├── <filename-2>.parquet
    │   │   └── ...
    └── manifests/
        ├── <manifest-file>.json  (points to data files)


s3://<bucket-name>/athena-iceberg-tables/<table-name>/
    ├── metadata/
    ├── data/
    └── manifests/

s3://<bucket-name>/s3tables-iceberg/<table-name>/
    ├── metadata/
    ├── data/
    └── manifests/

Data Format: Both methods (Iceberg in Athena and S3 Tables with Iceberg) store data in Parquet format.
Metadata Management: Metadata is handled by AWS Glue or S3 API, tracking table schema, snapshots, and partitioning.
Directory Structure: Data is stored under partitioned directories for optimal querying and performance, while metadata is stored in metadata/ and manifests/ directories.
